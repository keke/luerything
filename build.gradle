//build.gradle

apply from: 'gradle/extra-tasks.gradle'

buildscript{
  
}

allprojects {
  apply plugin: 'eclipse'
  apply plugin: 'idea'
}

subprojects{
  group = rootProject.group
  repositories {
    mavenCentral()
  }
}